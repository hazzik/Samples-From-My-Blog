@model Application.DatePicker.Solution3.Form3
<script type="text/javascript">
    (function ($) {
        $(function () {
            $('[role=datepicker],[type=date]').each(function () {
                if (this.type === 'date') return;
                self.datepicker(self.data());
            });
        });
    })(jQuery);
</script>
<div>
    <a href="@Url.Action("Index")">Домой</a>
</div>
<div>
    @using(Html.BeginForm()) 
    {
        @Html.EditorForModel()
        
        <input type="submit"/>
    }
</div>
<div>
    <a href="@Url.Action("Index")">Домой</a>
</div>